name: Deploy QA

on:
  push:
    branches:
      - qa

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Terraform Init
        run: |
          cd terraform/environments/qa
          terraform init

      - name: Terraform Validate
        run: |
          cd terraform/environments/qa
          terraform validate

      - name: Terraform Plan
        run: |
          cd terraform/environments/qa
          terraform plan -var-file=qa.tfvars

      - name: Terraform Apply
        run: |
          cd terraform/environments/qa
          terraform apply -auto-approve -var-file=qa.tfvars

      - name: Login To Amazon ECR
        run: |
          aws ecr get-login-password \
          | docker login \
          --username AWS \
          --password-stdin ${{ secrets.ECR_URL }}

      - name: Build Frontend Image
        run: |
          docker build \
          -t frontend-qa \
          ./app/frontend

      - name: Build Backend Image
        run: |
          docker build \
          -t backend-qa \
          ./app/backend

      - name: Tag Frontend Image
        run: |
          docker tag frontend-qa:latest \
          ${{ secrets.QA_FRONTEND_REPO }}:latest

      - name: Tag Backend Image
        run: |
          docker tag backend-qa:latest \
          ${{ secrets.QA_BACKEND_REPO }}:latest

      - name: Push Frontend Image
        run: |
          docker push \
          ${{ secrets.QA_FRONTEND_REPO }}:latest

      - name: Push Backend Image
        run: |
          docker push \
          ${{ secrets.QA_BACKEND_REPO }}:latest

      - name: Deploy Backend To QA
        run: |
          aws ssm send-command \
            --instance-ids "${{ secrets.QA_INSTANCE_ID }}" \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=[
              "aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin '${{ secrets.ECR_URL }}'",
              "docker pull '${{ secrets.QA_BACKEND_REPO }}':latest",
              "docker stop backend || true",
              "docker rm backend || true",
              "docker run -d --name backend -p 5000:5000 '${{ secrets.QA_BACKEND_REPO }}':latest"
            ]'

      - name: Health Check
        run: |
          sleep 30
          echo "QA Deployment Completed"